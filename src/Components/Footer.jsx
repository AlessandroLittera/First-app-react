import React from 'react';
import PropTypes from 'prop-types';

const Footer = props => {
    return (
        <div>
            FOOTER
        </div>
    );
};

Footer.propTypes = {
    
};

export default Footer;